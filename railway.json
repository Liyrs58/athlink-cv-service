{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install --no-cache-dir -r requirements.txt && pip uninstall -y opencv-python opencv-python-headless && pip install --no-cache-dir opencv-python-headless"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
