[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "libffi", "zlib", "ffmpeg", "glib", "libGL", "xorg.libSM", "xorg.libXext", "xorg.libXrender"]

[phases.install]
cmds = ["python3.11 -m pip install --upgrade pip", "python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 main.py"
